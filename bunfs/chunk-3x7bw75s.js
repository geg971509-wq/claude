// @bun @bytecode
// Claude Code is a Beta product per Anthropic's Commercial Terms of Service.
// By using Claude Code, you agree that all code acceptance or rejection decisions you make,
// and the associated conversations in context, constitute Feedback under Anthropic's Commercial Terms,
// and may be used to improve Anthropic's products, including training models.
// You are responsible for reviewing any code suggestions before use.

// (c) Anthropic PBC. All rights reserved. Use is subject to the Legal Agreements outlined here: https://code.claude.com/docs/en/legal-and-compliance.

// Version: 2.1.252
import "/$bunfs/root/chunk-qq1mdtb5.js";
import { ne } from "/$bunfs/root/chunk-tx16jn0x.js";
import "/$bunfs/root/chunk-gcks6mn0.js";
import { zt } from "/$bunfs/root/chunk-qm65zb83.js";
import "/$bunfs/root/chunk-qcx34e4j.js";
import "/$bunfs/root/chunk-gtv2468w.js";
import "/$bunfs/root/chunk-vfy57cpd.js";
import "/$bunfs/root/chunk-4xj01xwv.js";
import { Ze, R, l, os } from "/$bunfs/root/chunk-ypdw393e.js";
import { J, G, LJ } from "/$bunfs/root/chunk-f9h0bg01.js";
import { b, V } from "/$bunfs/root/chunk-fv016jr6.js";
import "/$bunfs/root/chunk-qk7r0t2g.js";
import { ce } from "/$bunfs/root/chunk-4ddxwr9r.js";
import { m } from "/$bunfs/root/chunk-bzx56g36.js";
import "/$bunfs/root/chunk-eqdctte5.js";
import { a } from "/$bunfs/root/chunk-fec4384a.js";
import "/$bunfs/root/chunk-wkxx62a2.js";
import "/$bunfs/root/chunk-0pgyw7te.js";
import "/$bunfs/root/chunk-ps6pc7xd.js";
import "/$bunfs/root/chunk-386w1sy8.js";
import "/$bunfs/root/chunk-r53tkxrh.js";
import "/$bunfs/root/chunk-ca80fke8.js";
import "/$bunfs/root/chunk-ax6dy60b.js";
import "/$bunfs/root/chunk-6k63g5t6.js";
import "/$bunfs/root/chunk-4fwj3vnx.js";
import "/$bunfs/root/chunk-nqmqabr8.js";
import "/$bunfs/root/chunk-q14dgq5g.js";
import "/$bunfs/root/chunk-f5hrzy3k.js";
import "/$bunfs/root/chunk-s7e0px8j.js";
import "/$bunfs/root/chunk-s28wf80n.js";
import "/$bunfs/root/chunk-3bbym8ct.js";
import "/$bunfs/root/chunk-cf8qhmdc.js";
import "/$bunfs/root/chunk-2masxyqj.js";
import "/$bunfs/root/chunk-rgw52f13.js";
import "/$bunfs/root/chunk-2rx5nghb.js";
import "/$bunfs/root/chunk-z0z1xzkg.js";
import "/$bunfs/root/chunk-c47snwm2.js";
import "/$bunfs/root/chunk-jpf4kat5.js";
import "/$bunfs/root/chunk-s4vc7est.js";
import "/$bunfs/root/chunk-hyh5wcm1.js";
import "/$bunfs/root/chunk-ntyhd04p.js";
import "/$bunfs/root/chunk-8qsdea2c.js";
import "/$bunfs/root/chunk-988p40e0.js";
import "/$bunfs/root/chunk-0spqrdaj.js";
import "/$bunfs/root/chunk-5rt2mvvk.js";
import "/$bunfs/root/chunk-1yr12dqr.js";
import "/$bunfs/root/chunk-2mmw62jx.js";
import "/$bunfs/root/chunk-z2bvp3sv.js";
import "/$bunfs/root/chunk-keb644xg.js";
import "/$bunfs/root/chunk-bj904w9w.js";
import "/$bunfs/root/chunk-18bck2pp.js";
import "/$bunfs/root/chunk-dwwpyy7b.js";
import "/$bunfs/root/chunk-jck8t8fq.js";
import "/$bunfs/root/chunk-psdymar6.js";
import "/$bunfs/root/chunk-0r9wzh7g.js";
import "/$bunfs/root/chunk-sxxp6vcr.js";
import "/$bunfs/root/chunk-rf51999f.js";
import { Vn, bt, Rc, Fl, Yt, ym, qa, T4t } from "/$bunfs/root/chunk-8tgj5dp2.js";
import { FT } from "/$bunfs/root/chunk-82w4mtvq.js";
import "/$bunfs/root/chunk-amem41jf.js";
import "/$bunfs/root/chunk-ghara6r1.js";
import "/$bunfs/root/chunk-97wwdfyx.js";
import "/$bunfs/root/chunk-e7rq8w09.js";
import "/$bunfs/root/chunk-v7yhn7cf.js";
import "/$bunfs/root/chunk-fbp3w46j.js";
import "/$bunfs/root/chunk-z9ex1x2t.js";
import "/$bunfs/root/chunk-k4a21mkt.js";
import "/$bunfs/root/chunk-4rh74qms.js";
import "/$bunfs/root/chunk-r3qa2s4k.js";
import "/$bunfs/root/chunk-1evht33z.js";
import "/$bunfs/root/chunk-gkfy5ggn.js";
import "/$bunfs/root/chunk-qk6zade1.js";
import "/$bunfs/root/chunk-4t3vsqt7.js";
import "/$bunfs/root/chunk-ejdwrhgv.js";
import "/$bunfs/root/chunk-zkvj62rn.js";
import "/$bunfs/root/chunk-492vgtnr.js";
import "/$bunfs/root/chunk-0g6cj4h1.js";
import "/$bunfs/root/chunk-a48rnvhj.js";
import "/$bunfs/root/chunk-tzhtxm67.js";
import "/$bunfs/root/chunk-mznd09c6.js";
import "/$bunfs/root/chunk-167jjb66.js";
import "/$bunfs/root/chunk-kxyny47c.js";
import "/$bunfs/root/chunk-4r03fm98.js";
import "/$bunfs/root/chunk-s4smmnta.js";
import { kt } from "/$bunfs/root/chunk-twwd9fep.js";
import "/$bunfs/root/chunk-fs02hzwy.js";
import "/$bunfs/root/chunk-dd55s64p.js";
import "/$bunfs/root/chunk-83h0j7w2.js";
import { he } from "/$bunfs/root/chunk-h605j25g.js";
import "/$bunfs/root/chunk-0m39033q.js";
import { zG, o2, rln, iT } from "/$bunfs/root/chunk-zze8764r.js";
import "/$bunfs/root/chunk-c6k0ecxv.js";
import "/$bunfs/root/chunk-2n1hsggr.js";
import "/$bunfs/root/chunk-s0xdwab3.js";
import "/$bunfs/root/chunk-c76q2y0e.js";
import "/$bunfs/root/chunk-cds31m36.js";
import "/$bunfs/root/chunk-z15hpjf9.js";
import "/$bunfs/root/chunk-1pn2pk9v.js";
import "/$bunfs/root/chunk-hebj76vj.js";
import "/$bunfs/root/chunk-0mrbtv3q.js";
import "/$bunfs/root/chunk-5n1tbe50.js";
import "/$bunfs/root/chunk-a2an01ph.js";
import "/$bunfs/root/chunk-dck778n3.js";
import "/$bunfs/root/chunk-nkjrjh5n.js";
import "/$bunfs/root/chunk-bp3j8sq3.js";
import "/$bunfs/root/chunk-qekzccch.js";
import "/$bunfs/root/chunk-s2ac0z44.js";
import "/$bunfs/root/chunk-wgz3g40p.js";
import "/$bunfs/root/chunk-1mp2j0tx.js";
import "/$bunfs/root/chunk-6a5ggyw4.js";
import "/$bunfs/root/chunk-btcxpqw5.js";
import "/$bunfs/root/chunk-k8xha1qv.js";
import "/$bunfs/root/chunk-30e2jew7.js";
import "/$bunfs/root/chunk-51qp0385.js";
import "/$bunfs/root/chunk-wjr8v4tx.js";
import "/$bunfs/root/chunk-kbyvkqye.js";
import "/$bunfs/root/chunk-37pvmyqb.js";
import "/$bunfs/root/chunk-qg6z8ah8.js";
import "/$bunfs/root/chunk-308krgtb.js";
import "/$bunfs/root/chunk-nf8aaj8a.js";
import "/$bunfs/root/chunk-a6xhjq7b.js";
import "/$bunfs/root/chunk-qnrh4abv.js";
import "/$bunfs/root/chunk-6nmk3t6g.js";
import "/$bunfs/root/chunk-jwrz2gmg.js";
import "/$bunfs/root/chunk-6w72z84m.js";
import "/$bunfs/root/chunk-nkf7ctf5.js";
import "/$bunfs/root/chunk-38mxah2g.js";
import "/$bunfs/root/chunk-z9bhq3k9.js";
import "/$bunfs/root/chunk-v94qfg1j.js";
import "/$bunfs/root/chunk-98tgyb1d.js";
import "/$bunfs/root/chunk-h7dkgq01.js";
import "/$bunfs/root/chunk-tveh0k24.js";
import "/$bunfs/root/chunk-699y5x28.js";
import "/$bunfs/root/chunk-da857b4g.js";
import "/$bunfs/root/chunk-anwgkcw5.js";
import "/$bunfs/root/chunk-cfqs035d.js";
import "/$bunfs/root/chunk-eb5y4yrx.js";
import "/$bunfs/root/chunk-rt5p4yax.js";
import "/$bunfs/root/chunk-vzc4dhqq.js";
import "/$bunfs/root/chunk-gvnqpbsf.js";
import "/$bunfs/root/chunk-5wecjdtm.js";
import "/$bunfs/root/chunk-5g92v3fd.js";
import "/$bunfs/root/chunk-x06p1jhb.js";
import "/$bunfs/root/chunk-vq9g7y26.js";
import "/$bunfs/root/chunk-e17gvevg.js";
import "/$bunfs/root/chunk-p1awcxk1.js";
import "/$bunfs/root/chunk-vx3kc9d2.js";
import "/$bunfs/root/chunk-1v3848mh.js";
import "/$bunfs/root/chunk-fctnm902.js";
import "/$bunfs/root/chunk-e53y7x75.js";
import "/$bunfs/root/chunk-tz5bx4mb.js";
import "/$bunfs/root/chunk-hz299nfa.js";
import "/$bunfs/root/chunk-w3c6n7jh.js";
import "/$bunfs/root/chunk-96xrtyd5.js";
import "/$bunfs/root/chunk-5c5qq1s7.js";
import "/$bunfs/root/chunk-trte0v0g.js";
import "/$bunfs/root/chunk-rgh8hav6.js";
import "/$bunfs/root/chunk-50g10fz0.js";
import "/$bunfs/root/chunk-gmgmb4mg.js";
import "/$bunfs/root/chunk-hzjjsb0j.js";
import "/$bunfs/root/chunk-avrc9gay.js";
import "/$bunfs/root/chunk-pbd9yggs.js";
import "/$bunfs/root/chunk-cgy998p5.js";
import "/$bunfs/root/chunk-xgfepdf4.js";
import "/$bunfs/root/chunk-r4m4cqz1.js";
import "/$bunfs/root/chunk-mq8h2tk8.js";
import "/$bunfs/root/chunk-a9vrr0nr.js";
import "/$bunfs/root/chunk-4sw5yxwb.js";
import "/$bunfs/root/chunk-d1d8xqks.js";
import "/$bunfs/root/chunk-91315wj5.js";
import "/$bunfs/root/chunk-8td9ks9h.js";
import "/$bunfs/root/chunk-9y48by1e.js";
import "/$bunfs/root/chunk-9tey6wcd.js";
import "/$bunfs/root/chunk-qd35gw0c.js";
import "/$bunfs/root/chunk-86v6b72m.js";
import "/$bunfs/root/chunk-c2kqvv3h.js";
import "/$bunfs/root/chunk-fvkzpeqe.js";
import "/$bunfs/root/chunk-k7v7xwvs.js";
import "/$bunfs/root/chunk-pzvxycbq.js";
import "/$bunfs/root/chunk-dzv4k86d.js";
import "/$bunfs/root/chunk-jn7d47r1.js";
import "/$bunfs/root/chunk-d5s7a9by.js";
import "/$bunfs/root/chunk-76gryyye.js";
import "/$bunfs/root/chunk-dc4hb8f7.js";
import "/$bunfs/root/chunk-z9jgt14c.js";
import "/$bunfs/root/chunk-rsp0anqd.js";
import "/$bunfs/root/chunk-y8sx7bf9.js";
import "/$bunfs/root/chunk-q2gyteg0.js";
import "/$bunfs/root/chunk-0manfhn2.js";
import "/$bunfs/root/chunk-r6ppzf4r.js";
import "/$bunfs/root/chunk-4399d2v0.js";
import "/$bunfs/root/chunk-y79sc2hm.js";
import "/$bunfs/root/chunk-2t3rnt88.js";
import "/$bunfs/root/chunk-986hvegp.js";
import "/$bunfs/root/chunk-8jsj2mx0.js";
import "/$bunfs/root/chunk-dt5bt1kc.js";
import "/$bunfs/root/chunk-z9rrv31w.js";
import "/$bunfs/root/chunk-5djs3wbb.js";
import "/$bunfs/root/chunk-pdf5n9dy.js";
import "/$bunfs/root/chunk-rntp2fy5.js";
import "/$bunfs/root/chunk-p18e2918.js";
import "/$bunfs/root/chunk-nfmyt8cw.js";
import "/$bunfs/root/chunk-6var9pwd.js";
import "/$bunfs/root/chunk-gmj1vprd.js";
import "/$bunfs/root/chunk-ht15rw3x.js";
import "/$bunfs/root/chunk-gay9hbs8.js";
import "/$bunfs/root/chunk-mvz9fsh3.js";
import "/$bunfs/root/chunk-ddbk6aje.js";
import "/$bunfs/root/chunk-krety1hw.js";
import "/$bunfs/root/chunk-6ywvv1q0.js";
import "/$bunfs/root/chunk-wn1jrcrk.js";
import "/$bunfs/root/chunk-4c3hecm4.js";
import "/$bunfs/root/chunk-zk5v8tw6.js";
import "/$bunfs/root/chunk-kdtcyk0z.js";
import "/$bunfs/root/chunk-q75cm02t.js";
import "/$bunfs/root/chunk-dxqac26z.js";
import "/$bunfs/root/chunk-2204t1q0.js";
import "/$bunfs/root/chunk-kfdrcvxr.js";
import "/$bunfs/root/chunk-fd2h00ye.js";
import "/$bunfs/root/chunk-z6m3k8w5.js";
import "/$bunfs/root/chunk-vm4xrmr7.js";
import "/$bunfs/root/chunk-hkzwg35z.js";
import "/$bunfs/root/chunk-mkweh3a4.js";
import "/$bunfs/root/chunk-ky33xsw7.js";
import {
  HA,
  m_e,
  vWe,
  Nrn,
  LZ,
  MZ,
  NZ,
  Zst,
  Hae,
  oG,
  g_e,
  b6n,
  w6n,
  RWe,
  T6n,
  E6n,
} from "/$bunfs/root/chunk-7vpf91fq.js";
import "/$bunfs/root/chunk-z45ex17x.js";
import { gje, aZ, _Un, _W, yUn, U0e, hje, lZ, Jot, bUn, wUn } from "/$bunfs/root/chunk-ptdw8z49.js";
import { vhe, hUn } from "/$bunfs/root/chunk-3jb6a5vs.js";
import "/$bunfs/root/chunk-k70ej0cs.js";
import "/$bunfs/root/chunk-czh9aynm.js";
import { JUn } from "/$bunfs/root/chunk-5vzrk7mr.js";
import "/$bunfs/root/chunk-c59q3nnv.js";
import "/$bunfs/root/chunk-31xy83wr.js";
import "/$bunfs/root/chunk-6nsyrmhg.js";
import "/$bunfs/root/chunk-jjkewjsg.js";
import "/$bunfs/root/chunk-4r71nj50.js";
import "/$bunfs/root/chunk-3qvtfvqg.js";
import "/$bunfs/root/chunk-r7t0evh2.js";
import "/$bunfs/root/chunk-6xz0kht6.js";
import "/$bunfs/root/chunk-2am7kb6k.js";
import "/$bunfs/root/chunk-9kr4hqvs.js";
import "/$bunfs/root/chunk-xvwx3w83.js";
import "/$bunfs/root/chunk-y9akj3mq.js";
import "/$bunfs/root/chunk-gdk3p0b3.js";
import "/$bunfs/root/chunk-qy75zxb2.js";
import "/$bunfs/root/chunk-r6t6p0zm.js";
import "/$bunfs/root/chunk-qpj83ec7.js";
import "/$bunfs/root/chunk-f77e8yef.js";
import "/$bunfs/root/chunk-f7y0mdt6.js";
import "/$bunfs/root/chunk-jb2pzt3t.js";
import "/$bunfs/root/chunk-9yzzw213.js";
import "/$bunfs/root/chunk-mknyj0qp.js";
import "/$bunfs/root/chunk-81defen8.js";
import "/$bunfs/root/chunk-mpg6r2j0.js";
import "/$bunfs/root/chunk-3dh32z3c.js";
import "/$bunfs/root/chunk-n8ecp4gs.js";
import "/$bunfs/root/chunk-406nxetx.js";
import "/$bunfs/root/chunk-2zf9m1zh.js";
import "/$bunfs/root/chunk-rxqmmxnb.js";
import "/$bunfs/root/chunk-gdhqsw8a.js";
import "/$bunfs/root/chunk-5xr818a0.js";
import "/$bunfs/root/chunk-fgavb5x0.js";
import "/$bunfs/root/chunk-sqa0bs5e.js";
import "/$bunfs/root/chunk-fveqn4y7.js";
import "/$bunfs/root/chunk-4zsxhey6.js";
import "/$bunfs/root/chunk-61nasexv.js";
import "/$bunfs/root/chunk-m1andcps.js";
import "/$bunfs/root/chunk-61hbq2wc.js";
import "/$bunfs/root/chunk-rq65wazg.js";
import "/$bunfs/root/chunk-5tdaspnd.js";
import "/$bunfs/root/chunk-rr0yj5sq.js";
import "/$bunfs/root/chunk-a7wreksn.js";
import "/$bunfs/root/chunk-8sbwe0jk.js";
import "/$bunfs/root/chunk-nwb64bac.js";
import "/$bunfs/root/chunk-g5y4mxg8.js";
import "/$bunfs/root/chunk-cbn30h5b.js";
import "/$bunfs/root/chunk-g01aac1k.js";
import "/$bunfs/root/chunk-3fwkz19s.js";
import "/$bunfs/root/chunk-9319g175.js";
import "/$bunfs/root/chunk-szz73wxx.js";
import "/$bunfs/root/chunk-b40bn863.js";
import "/$bunfs/root/chunk-6mgs10s2.js";
import "/$bunfs/root/chunk-6s9ybdq1.js";
import "/$bunfs/root/chunk-74qqwjhx.js";
import { i, q, _e, H, f, ot, un, De } from "/$bunfs/root/chunk-saay52v7.js";
import "/$bunfs/root/chunk-1hh0bg14.js";
import "/$bunfs/root/chunk-052zvbpg.js";
import "/$bunfs/root/chunk-werv747a.js";
import "/$bunfs/root/chunk-jzh4kg5x.js";
import "/$bunfs/root/chunk-7s7jqj2f.js";
import "/$bunfs/root/chunk-56sxk8k2.js";
import { Cg } from "/$bunfs/root/chunk-fxb0gsq0.js";
import { O } from "/$bunfs/root/chunk-dqkj2bph.js";
import "/$bunfs/root/chunk-7jz6r17g.js";
import "/$bunfs/root/chunk-a4q326ap.js";
function F(e) {
  return Buffer.byteLength(e, "utf8");
}
var M = {
    list_design_systems: { readOnly: !0, destructive: !1 },
    get_claude_design_prompt: { readOnly: !0, destructive: !1 },
    list_projects: { readOnly: !0, destructive: !1 },
    get_project: { readOnly: !0, destructive: !1 },
    list_files: { readOnly: !0, destructive: !1 },
    read_file: { readOnly: !0, destructive: !1 },
    get_conversation: { readOnly: !0, destructive: !1 },
    list_members: { readOnly: !0, destructive: !1 },
    render_preview: { readOnly: !1, destructive: !1 },
    create_project: { readOnly: !1, destructive: !1 },
    put_conversation: { readOnly: !1, destructive: !1 },
    finalize_plan: { readOnly: !1, destructive: !1 },
    write_files: { readOnly: !1, destructive: !1 },
    copy_files: { readOnly: !1, destructive: !1 },
    create_support_js: { readOnly: !1, destructive: !1 },
    add_member: { readOnly: !1, destructive: !1 },
    delete_files: { readOnly: !1, destructive: !0 },
    remove_member: { readOnly: !1, destructive: !0 },
    update_member_role: { readOnly: !1, destructive: !0 },
    update_sharing: { readOnly: !1, destructive: !0 },
  },
  U = {
    create_project: { top: new Set(["name", "design_system_id"]) },
    put_conversation: {
      top: new Set(["project_id", "title", "messages"]),
      arrays: { messages: { fields: new Set(["role", "content"]), required: new Set(["role", "content"]) } },
    },
    finalize_plan: {
      top: new Set(["project_id", "writes", "deletes", "scope"]),
      arrays: { writes: "string", deletes: "string" },
      scalarEnums: { scope: new Set(["paths", "project"]) },
    },
    write_files: {
      top: new Set(["project_id", "plan_token", "files"]),
      arrays: {
        files: { fields: new Set(["path", "data", "local_path", "encoding", "if_match"]), required: new Set(["path"]) },
      },
    },
    delete_files: {
      top: new Set(["project_id", "plan_token", "paths", "files"]),
      arrays: { paths: "string", files: { fields: new Set(["path", "if_match"]), required: new Set(["path"]) } },
    },
    copy_files: {
      top: new Set(["project_id", "plan_token", "files"]),
      arrays: {
        files: { fields: new Set(["src", "dest", "src_project_id", "if_match"]), required: new Set(["src", "dest"]) },
      },
    },
    render_preview: { top: new Set(["project_id", "path", "render", "validators"]), arrays: { validators: "string" } },
    create_support_js: { top: new Set(["project_id", "plan_token", "path", "if_match"]) },
    add_member: {
      top: new Set(["project_id", "account_uuid", "email", "role"]),
      scalarEnums: { role: { accepts: new Set(["viewer", "commenter", "editor"]), serverNormalizes: !0 } },
    },
    update_member_role: {
      top: new Set(["project_id", "account_uuid", "role"]),
      scalarEnums: { role: { accepts: new Set(["viewer", "commenter", "editor"]), serverNormalizes: !0 } },
    },
    remove_member: { top: new Set(["project_id", "account_uuid"]) },
    update_sharing: {
      top: new Set(["project_id", "scope", "link_permission"]),
      scalarEnums: {
        scope: { accepts: new Set(["invited", "org"]), serverNormalizes: !0 },
        link_permission: { accepts: new Set(["view", "comment", "edit"]), serverNormalizes: !0 },
      },
    },
  },
  W = {
    list: { top: new Set(["full"]) },
    list_design_systems: { top: new Set() },
    get_claude_design_prompt: { top: new Set(["design_system_id", "project_id"]) },
    list_projects: { top: new Set() },
    get_project: { top: new Set(["project_id"]) },
    list_files: { top: new Set(["project_id", "path"]) },
    read_file: { top: new Set(["project_id", "path"]) },
    get_conversation: { top: new Set(["project_id", "chat_id"]) },
    list_members: { top: new Set(["project_id"]) },
  };
function ie(e, t) {
  let n = Object.hasOwn(U, e) ? U[e] : Object.hasOwn(W, e) ? W[e] : void 0;
  if (!n) {
    let o = x(e);
    if (o?.readOnly === !0) return null;
    if (o !== void 0)
      return `ClaudeDesign ${e}: the server reports this operation as write-capable, and this client version can't validate its arguments (it needs a WRITE_OP_SCHEMAS entry). Update Claude Code to use it.`;
    return `ClaudeDesign ${e}: unrecognized operation. If the server added it recently, call {operation: "list"} first \u2014 a read-only operation becomes callable after discovery; a write-tier operation needs a WRITE_OP_SCHEMAS entry in this client.`;
  }
  let r = `ClaudeDesign ${e}`,
    c = (o) => (o.length > 60 ? o.slice(0, 60) + "\u2026" : o);
  if (e === "finalize_plan" && t?.scope === "project" && (t.writes !== void 0 || t.deletes !== void 0))
    return `${r}: scope "project" takes no writes/deletes \u2014 a project-scoped plan covers every path in the project.`;
  for (let [o, u] of Object.entries(t ?? {})) {
    if (!n.top.has(o)) {
      let s = [...n.top].find((g) => g.toLowerCase() === o.toLowerCase()),
        d = [...n.top].join(", ");
      return `${r}: unrecognized argument '${c(o)}'${s ? ` (did you mean '${s}'?)` : ""}. ${d ? `Allowed: ${d}.` : "This operation takes no arguments."}`;
    }
    if (!(o in (n.arrays ?? {})) && typeof u === "object" && u !== null)
      return `${r}.${o}: must be a scalar (string/bool/number), not ${Array.isArray(u) ? "an array" : "an object"}.`;
    let _ = o === "plan_token" ? 65536 : 4096;
    if (typeof u === "string" && F(u) > _) return `${r}.${o}: too long (${F(u)} bytes; capped at ${_}).`;
    let y = "scalarEnums" in n ? n.scalarEnums?.[o] : void 0;
    if (y !== void 0) {
      let s = "accepts" in y,
        d = s ? y.accepts : y,
        g = s && typeof u === "string" ? u.trim().toLowerCase() : u;
      if (typeof g !== "string" || !d.has(g))
        return `${r}.${o}: must be one of ${[...d].map((w) => `"${w}"`).join(", ")}${s ? " (case-insensitive)" : ""}.`;
      if (s && typeof u === "string" && u.length > g.length + 2)
        return `${r}.${o}: excess whitespace around the value.`;
    }
  }
  for (let [o, u] of Object.entries(n.arrays ?? {})) {
    let _ = t?.[o];
    if (_ === void 0) continue;
    if (!Array.isArray(_)) return `${r}.${o}: must be an array.`;
    for (let y = 0; y < _.length; y++) {
      let s = _[y];
      if (u === "string") {
        if (typeof s !== "string") return `${r}.${o}[${y}]: must be a string.`;
        if (F(s) > 4096) return `${r}.${o}[${y}]: too long (${F(s)} bytes; capped at 4096).`;
      } else {
        if (typeof s !== "object" || s === null || Array.isArray(s))
          return `${r}.${o}[${y}]: must be an object with keys from {${[...u.fields].join(", ")}}.`;
        for (let d of u.required)
          if (typeof s[d] !== "string") return `${r}.${o}[${y}]: missing required field '${d}'.`;
        for (let [d, g] of Object.entries(s)) {
          if (!u.fields.has(d)) {
            let w = [...u.fields].find((k) => k.toLowerCase() === d.toLowerCase());
            return `${r}.${o}[${y}]: unrecognized field '${c(d)}'${w ? ` (did you mean '${w}'?)` : d === "content" ? " (did you mean 'data'? designmcp's write_files reads 'data', not 'content')" : ""}. Allowed: ${[...u.fields].join(", ")}.`;
          }
          if (typeof g !== "string")
            return `${r}.${o}[${y}].${d}: must be a string (got ${Array.isArray(g) ? "array" : typeof g}).`;
          if (d !== "data" && d !== "content" && F(g) > 4096)
            return `${r}.${o}[${y}].${d}: too long (${F(g)} bytes; non-body fields are capped at 4096).`;
        }
      }
    }
  }
  return null;
}
function K(e) {
  return Object.hasOwn(M, e) ? M[e] : void 0;
}
var je = m(() =>
  un({
    name: i(),
    description: i().optional(),
    inputSchema: _e().optional(),
    annotations: un({ readOnlyHint: q().optional(), destructiveHint: q().optional() }).optional(),
  }),
);
class oe {
  byOperation = new Map();
  hintsFor(e) {
    return this.byOperation.get(e);
  }
  knows(e) {
    return this.byOperation.has(e);
  }
  record(e, t) {
    this.byOperation.set(e, t);
  }
  reset() {
    this.byOperation.clear();
  }
}
var Ce = new J(() => new oe());
function te() {
  return Ce.of(G().host);
}
function x(e) {
  return te().hintsFor(e);
}
function le(e) {
  let t = te();
  t.reset();
  let n = [];
  for (let r of e) {
    let c = je().safeParse(r);
    if (!c.success) continue;
    let o = c.data,
      u = Object.hasOwn(M, o.name) ? M[o.name] : void 0,
      _ = o.annotations?.readOnlyHint ?? u?.readOnly ?? !1,
      y = o.annotations?.destructiveHint ?? u?.destructive ?? !0;
    if (u) (_ &&= u.readOnly), (y ||= u.destructive);
    t.record(o.name, { readOnly: _, destructive: y }),
      n.push({
        name: o.name,
        description: o.description ?? "",
        inputSchema: o.inputSchema ?? {},
        readOnlyHint: _,
        destructiveHint: y,
      });
  }
  return n;
}
function de(e) {
  return !te().knows(e);
}
function L(e, t) {
  if (!t) return e;
  return e.split(t).join("[redacted-oauth-token]");
}
function Ae(e) {
  return {
    Authorization: `Bearer ${e}`,
    "Content-Type": "application/json",
    "anthropic-version": "2023-06-01",
    "anthropic-client-platform": Cg(),
  };
}
function E(e) {
  return e < 1024 ? `${e}B` : `${(e / 1024).toFixed(1)}KB`;
}
function N(e) {
  return Buffer.byteLength(e, "utf8");
}
function $e(e) {
  switch (e) {
    case "list":
      return "List operations";
    case "list_design_systems":
      return "List design systems";
    case "get_claude_design_prompt":
      return "Get design prompt";
    case "list_projects":
      return "List projects";
    case "get_project":
      return "Read project metadata";
    case "list_files":
      return "List project files";
    case "read_file":
      return "Read file";
    case "get_conversation":
      return "Read conversation";
    case "list_members":
      return "List project members";
    case "create_project":
      return "Create project";
    case "put_conversation":
      return "Write conversation";
    case "finalize_plan":
      return "Finalize plan";
    case "write_files":
      return "Write files";
    case "copy_files":
      return "Copy files";
    case "delete_files":
      return "Delete files";
    case "render_preview":
      return "Render preview";
    case "create_support_js":
      return "Create support.js";
    case "add_member":
      return "Add project member";
    case "update_member_role":
      return "Update member role";
    case "remove_member":
      return "Remove project member";
    case "update_sharing":
      return "Update sharing";
    default:
      return e ?? "";
  }
}
var Re = m(() =>
    ot({
      operation: i()
        .regex(/^[\w.-]{1,64}$/)
        .describe(
          `Claude Design action to perform. Call with "${o2}" first to discover the available operations and their argument schemas.`,
        ),
      arguments: De(i(), _e())
        .default({})
        .describe("Action input object (server-validated). Pass {} for operations that take no input."),
    }),
  ),
  Ie = m(() => f({ operation: i(), content: H(De(i(), _e())), isError: q().optional() }));
function re(e) {
  let t = he(e);
  return (
    !e.options?.isNonInteractiveSession &&
    t.mode !== "bypassPermissions" &&
    !(t.mode === "plan" && t.isBypassPermissionsModeAvailable)
  );
}
var ue =
    "Approving also lets writes and deletes to exactly these paths run without another prompt for up to 15 minutes (file contents are not shown again; anything to any other path will still ask).",
  pe = { invited: "private: invited members only", org: "visible to your whole organization", public: "PUBLIC" },
  Ee = 60,
  fe =
    /[\u0000-\u001F\u007F-\u009F\u00AD\u061C\u180E\u200B\u200E\u200F\u202A-\u202E\u2060-\u2064\u2066-\u2069\u2026\u2800\u3164\uFE00-\uFE0E\uFEFF\uFFF9-\uFFFB\uFFA0]/;
function Oe(e) {
  return (
    /(?:^|\s)[\u200C\u200D\uFE0F]/.test(e) ||
    /[\u200C\u200D](?=\s|$)/.test(e) ||
    /[\u200C\u200D][\u200C\u200D]/.test(e) ||
    /(?:^|[^\p{Emoji}])\uFE0F/u.test(e)
  );
}
var ge = /\uDB40[\uDC20-\uDC7F\uDD00-\uDDEF]|\uD834[\uDD73-\uDD7A]/,
  me =
    /["\u201C\u201D\u201E\u201F\uFF02\u2033\u2036\u02BA\u02DD\u02EE\u05F4\u3003\u301D-\u301F\u275D\u275E\u2014\u2015]/;
function B(e) {
  if (e.agentContext?.agentType !== "main") return !1;
  try {
    return !iT("PermissionRequest", e.sessionHooksRegistry, e.agentContext?.agentId ?? "");
  } catch {
    return !1;
  }
}
async function Te(e, t, n, r) {
  try {
    let c = await U0e(r);
    if (c.ok !== !0) return null;
    let o = await be(e, "get_project", { project_id: t }, c.accessToken, n, null, r);
    if (o.isError === !0) return null;
    for (let u of o.content)
      if (u?.type === "text" && typeof u.text === "string") {
        let _ = V(u.text),
          y = _.name,
          s = _.url,
          d = _.sharing?.scope;
        if (typeof y !== "string") return null;
        let g = y.replace(/\s+/g, " ").trim();
        if (g.length === 0 || fe.test(g) || ge.test(g) || Oe(g) || g.includes("://")) return null;
        if (me.test(g)) return null;
        if (typeof d !== "string" || !Object.hasOwn(pe, d)) return null;
        if (typeof s !== "string") return null;
        let w;
        try {
          w = new URL(s);
        } catch {
          return null;
        }
        if (
          w.protocol !== "https:" ||
          !w.pathname.endsWith(`/p/${t}`) ||
          w.username !== "" ||
          w.password !== "" ||
          w.search !== "" ||
          w.hash !== "" ||
          s.length > 200 ||
          /\s/.test(s) ||
          me.test(s) ||
          fe.test(s) ||
          ge.test(s) ||
          /[\u200C\u200D\uFE0F]/.test(s)
        )
          return null;
        let k = ce(g, Ee);
        return { name: k === g ? g : `${k.trimEnd()}\u2026`, sharingLabel: pe[d], url: s };
      }
    return null;
  } catch {
    if (n.aborted) throw new Ze();
    return null;
  }
}
var $t = kt({
    name: zG,
    searchHint: "work with Claude Design (claude.ai/design) projects",
    maxResultSizeChars: 1e5,
    get inputSchema() {
      return Re();
    },
    get outputSchema() {
      return Ie();
    },
    isEnabled: vhe,
    isConcurrencySafe(e) {
      if (e.operation === o2) return !0;
      return (x(e.operation) ?? K(e.operation))?.readOnly === !0;
    },
    isReadOnly(e) {
      if (e.operation === o2) return !0;
      return (x(e.operation) ?? K(e.operation))?.readOnly === !0;
    },
    async description() {
      return rln;
    },
    async prompt() {
      return rln;
    },
    userFacingName(e) {
      let t = $e(e?.operation);
      return t ? `Claude Design: ${t}` : "Claude Design";
    },
    toAutoClassifierInput(e) {
      return { operation: e.operation, arguments: e.arguments ?? {} };
    },
    async validateInput(e) {
      let t = ie(e.operation, e.arguments);
      if (t) return { result: !1, message: t, errorCode: 1 };
      if (e.operation === "finalize_plan") {
        let n = e.arguments?.project_id;
        if (typeof n !== "string" || n.length === 0)
          return {
            result: !1,
            message: "ClaudeDesign finalize_plan: project_id is required (a plan is always scoped to one project).",
            errorCode: 1,
          };
        if (!/^[A-Za-z0-9._-]+$/.test(n))
          return {
            result: !1,
            message:
              "ClaudeDesign finalize_plan: project_id contains characters outside the server id charset (letters, digits, dot, underscore, dash).",
            errorCode: 1,
          };
        if (n.length > vWe - 2)
          return {
            result: !1,
            message: "ClaudeDesign finalize_plan: project_id is longer than any server-issued project id.",
            errorCode: 1,
          };
      }
      return { result: !0 };
    },
    async checkPermissions(e, t) {
      let n = t.toolState.get(HA),
        r = e.operation === o2 ? null : await hje(n, t.credentials),
        c = {
          ...e,
          __consentBitShown: r,
          __consentAskCanReachUser: re(t),
          __projectGrantAskShown: void 0,
          __projectGrantServerObserved: void 0,
          __reservedTargetsAskShown: void 0,
          __finalizePlanAskShown: void 0,
        },
        o = r !== null ? aZ(r) : null;
      if (e.operation === "finalize_plan" && e.arguments?.scope === "project")
        return {
          behavior: "deny",
          message:
            'ClaudeDesign finalize_plan: scope "project" is no longer supported by this client. Write files directly without plan_token \u2014 the first write to a project asks for a one-time durable approval \u2014 or use finalize_plan with writes/deletes for path-scoped plans and deletes.',
          decisionReason: {
            type: "safetyCheck",
            reason: 'finalize_plan scope:"project" is superseded by the durable per-project write grant',
            classifierApprovable: !1,
          },
        };
      let u = e.operation === o2 ? { readOnly: !0, destructive: !1 } : (x(e.operation) ?? K(e.operation)),
        _ = u?.readOnly !== !0,
        y = _ && u?.destructive !== !1;
      if (
        e.operation === "copy_files" &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      )
        return {
          behavior: "deny",
          message:
            "ClaudeDesign copy_files: copying without a plan_token always requires per-batch approval \u2014 use finalize_plan declaring every destination in writes, then pass the returned plan_token.",
          decisionReason: {
            type: "safetyCheck",
            reason:
              "tokenless copy_files is per-batch-only \u2014 its destinations have no reserved-path or render-integrity gate",
            classifierApprovable: !1,
          },
        };
      if (o) {
        if (
          (e.operation === "write_files" || e.operation === "create_support_js") &&
          !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
        ) {
          let g = RWe(e.operation, e.arguments);
          if (g.outcome !== "pass" || !Nrn(g.targets)) {
            let w = g.targets !== void 0 && g.targets.length === 0,
              k = g.outcome === "pass";
            return {
              behavior: "deny",
              message: w
                ? `ClaudeDesign ${e.operation}: this call names no target paths \u2014 list the files to write, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                : k
                  ? `ClaudeDesign ${e.operation}: this batch is too large to display fully for approval \u2014 split it into smaller batches, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                  : `ClaudeDesign ${e.operation}: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason: w
                  ? "a write batch naming no target paths \u2014 malformed call"
                  : k
                    ? "the batch cannot be rendered fully in the approval dialog \u2014 split it or use per-batch plans"
                    : "reserved or unenumerable target paths always require per-batch approval",
                classifierApprovable: !1,
              },
            };
          }
        }
        let s = _ ? `Design ${e.operation} writes to claude.ai/design.` : "",
          d = e.operation === "finalize_plan" ? ue : "";
        return {
          behavior: "ask",
          message: [o, s, d].filter(Boolean).join(" "),
          updatedInput: e.operation === "finalize_plan" ? { ...c, __finalizePlanAskShown: !0 } : c,
          localDisplayOnly: !0,
          decisionReason: {
            type: "safetyCheck",
            reason:
              "design agent consent \u2014 approving records a server-side grant for Claude agents to read and write your design projects",
            classifierApprovable: !1,
          },
        };
      }
      if (!_) return { behavior: "allow", updatedInput: c };
      if (he(t).mode !== "plan" && B(t) && T6n(n, e.operation, e.arguments).outcome === "allow")
        return { behavior: "allow", updatedInput: c };
      if (
        (e.operation === "write_files" || e.operation === "create_support_js") &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      ) {
        if (t.options?.isNonInteractiveSession === !0) {
          let d = {
              behavior: "deny",
              message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time interactive project approval, which is not available in non-interactive sessions \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason:
                  "a durable project write grant requires an interactive approval with a server-verified project identity",
                classifierApprovable: !1,
              },
            },
            g = e.arguments?.project_id;
          if (typeof g !== "string" || g.length === 0 || !B(t) || oG(n, g)) return d;
          if (RWe(e.operation, e.arguments).outcome !== "pass") return d;
          if (he(t).mode === "plan") return d;
          if (Zst(n, g)) return { behavior: "allow", updatedInput: c };
          if ((await Jot(n, g, t.credentials)) === "granted" && !oG(n, g)) {
            if ((NZ(n, g), he(t).mode !== "plan")) return { behavior: "allow", updatedInput: c };
          }
          return d;
        }
        if (!B(t))
          return {
            behavior: "deny",
            message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not available in subagent or PermissionRequest-hook sessions \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
            decisionReason: {
              type: "safetyCheck",
              reason:
                "a durable project write grant requires a context where the approval card reaches the user directly",
              classifierApprovable: !1,
            },
          };
        let s = e.arguments?.project_id;
        if (typeof s === "string" && s.length > 0) {
          if (b6n(n, s))
            return {
              behavior: "deny",
              message: `ClaudeDesign ${e.operation}: this project cannot hold a durable write grant for this account (it may be shared from another organization) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason: "the server refused to mint a durable grant for this project \u2014 per-batch plans only",
                classifierApprovable: !1,
              },
            };
          let d = RWe(e.operation, e.arguments),
            g = he(t).mode === "plan";
          if (d.outcome === "pass" && g) {
            if (!(Zst(n, s) || (!oG(n, s) && (await Jot(n, s, t.credentials)) === "granted" && !oG(n, s))))
              return {
                behavior: "deny",
                message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not shown in plan mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason: "a durable project write grant cannot be minted from plan mode \u2014 per-batch plans only",
                  classifierApprovable: !1,
                },
              };
            NZ(n, s);
          } else if (d.outcome === "pass") {
            if (Zst(n, s)) return { behavior: "allow", updatedInput: c };
            let w = await Jot(n, s, t.credentials);
            if (w === "granted" && !oG(n, s)) {
              if ((NZ(n, s), he(t).mode !== "plan")) return { behavior: "allow", updatedInput: c };
            } else if (w === "unavailable")
              return {
                behavior: "deny",
                message: `ClaudeDesign ${e.operation}: could not check for a project write grant (this server may not support durable grants) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason: "the grant state could not be verified \u2014 fail toward the per-batch plan flow",
                  classifierApprovable: !1,
                },
              };
            else {
              let k = () =>
                  he(t).mode !== "plan"
                    ? null
                    : {
                        behavior: "deny",
                        message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not shown in plan mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                        decisionReason: {
                          type: "safetyCheck",
                          reason:
                            "a durable project write grant cannot be minted from plan mode \u2014 per-batch plans only",
                          classifierApprovable: !1,
                        },
                      },
                A = k();
              if (A) return A;
              if (!re(t))
                return {
                  behavior: "deny",
                  message: `ClaudeDesign ${e.operation}: writing without a plan_token to a project without a write grant requires a one-time interactive approval, which cannot be shown in this permission mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                  decisionReason: {
                    type: "safetyCheck",
                    reason:
                      "a durable project write grant requires an interactive approval with a server-verified project identity",
                    classifierApprovable: !1,
                  },
                };
              let h = await Te(n, s, t.abortController.signal, t.credentials);
              if (h === null)
                return {
                  behavior: "deny",
                  message: `ClaudeDesign ${e.operation}: a durable project write grant is only offered when the approval dialog can name its target, and the project identity (name, sharing, URL) could not be verified or rendered faithfully. If this is a fresh connection, read the project first (e.g. get_project \u2014 approve the Claude Design connection if prompted) and retry once; otherwise use finalize_plan with writes/deletes and pass the returned plan_token (the per-batch flow), which is always supported.`,
                  decisionReason: {
                    type: "safetyCheck",
                    reason:
                      "a durable project write grant requires a server-verified project identity in the approval card",
                    classifierApprovable: !1,
                  },
                };
              let S = k();
              if (S) return S;
              return {
                behavior: "ask",
                message: `Approving writes the listed files now, and lets Claude write to ANY file in the project "${h.name}" (${h.sharingLabel}) \u2014 ${h.url} \u2014 without asking again. This approval is remembered for this project until you revoke it in settings at claude.ai/design (future writes and file contents are not shown for approval). Deletes and CLAUDE.md/.claude paths still ask every time.`,
                updatedInput: { ...c, __projectGrantAskShown: s },
                localDisplayOnly: !0,
                serverApprovalWatch: { kind: "design_project_grant", projectId: s },
                decisionReason: {
                  type: "safetyCheck",
                  reason:
                    "first write under a project grant \u2014 approval mints a durable write grant for this project, revocable at claude.ai/design settings",
                  classifierApprovable: !1,
                },
              };
            }
          } else {
            if (
              !(
                Nrn(d.targets) &&
                (Zst(n, s) || (!oG(n, s) && (await Jot(n, s, t.credentials)) === "granted" && !oG(n, s)))
              )
            ) {
              let A = d.targets !== void 0 && d.targets.length === 0;
              return {
                behavior: "deny",
                message: A
                  ? `ClaudeDesign ${e.operation}: this call names no target paths \u2014 list the files to write, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                  : `ClaudeDesign ${e.operation}: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason: A
                    ? "a write batch naming no target paths \u2014 malformed call"
                    : "reserved or unenumerable target paths always require per-batch approval",
                  classifierApprovable: !1,
                },
              };
            }
            return (
              NZ(n, s),
              {
                behavior: "ask",
                message: `Design ${e.operation} writes to claude.ai/design.`,
                updatedInput: { ...c, __reservedTargetsAskShown: !0 },
                localDisplayOnly: !0,
                decisionReason: {
                  type: "safetyCheck",
                  reason: y
                    ? "destructive \u2014 model-generated arguments"
                    : "remote write \u2014 model-generated arguments",
                  classifierApprovable: !1,
                },
              }
            );
          }
        }
      }
      if (e.operation === "finalize_plan")
        return {
          behavior: "ask",
          message: `Design finalize_plan writes to claude.ai/design. ${ue}`,
          updatedInput: { ...c, __finalizePlanAskShown: !0 },
          localDisplayOnly: !0,
          decisionReason: {
            type: "safetyCheck",
            reason:
              "finalize_plan \u2014 the human is the path-review boundary; approval also grants prompt-free writes to these paths for 15 min",
            classifierApprovable: !1,
          },
        };
      if (y)
        return {
          behavior: "ask",
          message: `Design ${e.operation} writes to claude.ai/design.`,
          updatedInput: c,
          decisionReason: {
            type: "safetyCheck",
            reason: u
              ? "destructive \u2014 model-generated arguments"
              : "unknown operation \u2014 fail-closed to destructive write",
            classifierApprovable: !1,
          },
        };
      return {
        behavior: "ask",
        message: `Design ${e.operation} writes to claude.ai/design.`,
        updatedInput: c,
        decisionReason: {
          type: "safetyCheck",
          reason: "remote write \u2014 model-generated arguments",
          classifierApprovable: !1,
        },
      };
    },
    async call(e, t) {
      let n = t.toolState.get(HA),
        r = e.__consentAskCanReachUser ?? !1,
        c = r && re(t);
      if (e.operation === "finalize_plan" && r) {
        let h = (e.arguments ?? {}).project_id;
        if (typeof h === "string" && h.length > 0) MZ(n, h);
      }
      if (LZ.has(e.operation)) {
        let h = (e.arguments ?? {}).project_id;
        if (typeof h === "string" && h.length > 0) MZ(n, h), m_e(n, h), Hae(n, h), g_e(n, h);
      }
      let o = await U0e(t.credentials);
      if (!o.ok)
        throw new R(Ve(o, { isNonInteractiveSession: t.options?.isNonInteractiveSession }), "design_tool_auth_failed");
      let u = o.accessToken,
        _ = e.__consentBitShown ?? null,
        y = e.__projectGrantAskShown ?? null,
        s = e.__projectGrantServerObserved === !0,
        d = (() => {
          if (e.operation !== "write_files" && e.operation !== "create_support_js") return null;
          let h = e.arguments ?? {};
          if (typeof h.plan_token === "string" && h.plan_token.length > 0) return null;
          let S = h.project_id;
          return typeof S === "string" && S.length > 0 ? S : null;
        })();
      if (
        e.operation === "copy_files" &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      )
        throw new R(
          "copy_files without a plan_token always requires per-batch approval \u2014 use finalize_plan declaring every destination in writes, then pass the returned plan_token.",
          "design_tool_exec_backstop_tokenless_copy",
        );
      if (d !== null) {
        let h = RWe(e.operation, e.arguments),
          S = e.__reservedTargetsAskShown === !0;
        if (h.outcome !== "pass" && !S)
          throw new R(
            `${e.operation} without a plan_token: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
            "design_tool_exec_backstop_safety_trip",
          );
      }
      let g = t.abortController.signal,
        w = t.agentContext?.agentId ?? null,
        k;
      try {
        if (((k = await A({})), e.operation === "finalize_plan" && e.__finalizePlanAskShown === !0 && c && B(t))) {
          let h = e.arguments ?? {},
            S = k.isError !== !0 ? E6n(k.content) : null,
            p = (v) => (Array.isArray(v) ? v.filter((C) => typeof C === "string") : []);
          if (S !== null && typeof h.project_id === "string" && h.scope !== "project")
            w6n(n, S.token, {
              projectId: h.project_id,
              writes: p(h.writes),
              deletes: p(h.deletes),
              serverExpiresAtMs: S.expiresAtMs ?? void 0,
            });
        }
      } catch (h) {
        if (g.aborted && !(h instanceof Ze)) throw new Ze();
        if (h instanceof ae) {
          let S = await We(t.credentials, {
            isNonInteractiveSession: t.options?.isNonInteractiveSession,
            wasRetried: h.wasRetried,
          });
          if (g.aborted) throw new Ze();
          throw new R(S, "design_tool_auth_401");
        }
        if (h instanceof Z && t.options?.isNonInteractiveSession) throw new Z({ isNonInteractiveSession: !0 });
        throw h;
      }
      return { data: k };
      async function A(h) {
        try {
          let S = await be(n, e.operation, e.arguments, u, g, w, t.credentials);
          if (_ !== null && !g.aborted && S.isError !== !0) _W(n, _, !0);
          if (d !== null && !g.aborted && S.isError !== !0 && ((y === d && !s) || !oG(n, d))) NZ(n, d);
          return S;
        } catch (S) {
          if (S instanceof Y) {
            if ((Hae(n, S.projectId), _Un(n), h.grant === !0)) throw S;
            if (S.projectId !== y || s) {
              if (e.operation === "write_files" || e.operation === "create_support_js") {
                if (t.options?.isNonInteractiveSession === !0)
                  throw new R(
                    "Writing to this project needs a project write grant that is not currently active (it may have been revoked) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token. A durable project write grant can be approved from an interactive Claude Code session.",
                    "design_tool_needs_project_grant_non_interactive",
                  );
                throw new R(
                  "Writing to this project needs a one-time approval \u2014 retry the write: you will be shown the approval, or routed to the per-batch finalize_plan flow.",
                  "design_tool_needs_project_grant_not_shown",
                );
              }
              throw new R(
                "This operation needs a plan_token here \u2014 use finalize_plan (listing the destination paths in writes) and pass the returned plan_token. (It can run without one only under an existing project write grant, set up by a write_files to this project.)",
                "design_tool_needs_project_grant_ineligible_op",
              );
            }
            if (!c || !B(t))
              throw new R(
                "Writing to this project needs a one-time approval, which cannot be granted automatically in this permission mode \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.",
                "design_tool_needs_project_grant_no_prompt",
              );
            try {
              await bUn(n, S.projectId, t.credentials);
            } catch (v) {
              if (os(v).kind === "auth")
                throw new R(
                  "This session's credential cannot record the project approval \u2014 approve this project from an interactive Claude Code session (run the write there and accept the approval card), then retry here: this session picks the recorded approval up from the server.",
                  "design_tool_grant_mint_bearer_refused",
                );
              throw v;
            }
            return A({ ...h, grant: !0 });
          }
          if (!(S instanceof X)) throw S;
          if (h.consent === !0) throw S;
          let p = S.consent;
          if (p !== _)
            throw (
              (_W(n, p, !1),
              new R(
                `${aZ(p)} The user hasn't granted this yet \u2014 ask them to retry (the prompt will show on the next call) or run /design consent.`,
                "design_tool_needs_consent_not_shown",
              ))
            );
          if (!c)
            throw (
              (_W(n, p, !1),
              new R(
                `${aZ(p)} The user hasn't granted this \u2014 run /design consent to grant it (it can't be approved automatically in this permission mode).`,
                "design_tool_needs_consent_no_prompt",
              ))
            );
          return await lZ(n, p, t.credentials), A({ ...h, consent: !0 });
        }
      }
    },
    mapToolResultToToolResultBlockParam(e, t) {
      let n = Fe(e.content.flatMap(Pe)),
        r = e.isError
          ? n
              .flatMap((c) => (c.type === "text" ? [c.text] : []))
              .join(`
`)
              .trim() || "(error with no message)"
          : null;
      return {
        tool_use_id: t,
        type: "tool_result",
        is_error: e.isError,
        content: r ?? (n.length > 0 ? n : "(empty result)"),
      };
    },
    renderToolUseMessage(e, { verbose: t }) {
      let r = (p, v = 80) => {
          let C = p.replace(/[\x00-\x1f\x7f-\x9f]+/g, " ");
          return C.length > v ? C.slice(0, v).trimEnd() + "\u2026" : C;
        },
        c = e.arguments;
      if (!c || Object.keys(c).length === 0) return "";
      if (!t) {
        let p = {};
        for (let [v, C] of Object.entries(c))
          if (Array.isArray(C)) {
            let j = v === "messages" ? "message" : v === "validators" ? "validator" : "file";
            p[j] = (p[j] ?? 0) + C.length;
          }
        return Object.entries(p)
          .map(([v, C]) => `${C} ${C === 1 ? v : v + "s"}`)
          .join(", ");
      }
      if (!Object.hasOwn(U, e.operation ?? "") && !Object.hasOwn(W, e.operation ?? "")) {
        let p = b(c) ?? "";
        return `${r(e.operation ?? "")} (${E(N(p))} of arguments not shown \u2014 operation unknown to this client version)`;
      }
      let o = (p, v) => {
          if (typeof p === "string") return p.length > 80 ? `${r(p)} (${E(N(p))})` : r(p);
          if (typeof p === "object" && p !== null) {
            let j = p,
              P = N(b(p) ?? "");
            if (typeof j.path === "string") {
              let D = typeof j.data === "string" ? j.data : "",
                T = ["encoding", "local_path", "if_match"].filter((z) => typeof j[z] === "string" && j[z]),
                I = `${r(j.path)} (${E(P)})${T.length ? ` [+${T.join(",")}]` : ""}`;
              return v > 0 && D ? `${I}: ${r(D, v)}` : I;
            }
            if (typeof j.content === "string") {
              let D = typeof j.role === "string" ? j.role : "?",
                T = `${r(D, 12)} (${E(P)})`;
              return v > 0 && j.content ? `${T}: ${r(j.content, v)}` : T;
            }
            if (typeof j.src === "string" || typeof j.dest === "string")
              return `${r(String(j.src ?? "?"), 40)} \u2192 ${r(String(j.dest ?? "?"), 40)}${j.src_project_id ? ` [from ${r(String(j.src_project_id), 24)}]` : ""}${typeof j.if_match === "string" && j.if_match ? " [+if_match]" : ""}${P > 256 ? ` (${E(P)})` : ""}`;
          }
          let C = b(p) ?? String(p);
          return `${r(C)} (${E(N(C))})`;
        },
        u = (p) =>
          typeof p === "string"
            ? r(p, 40)
            : typeof p === "object" && p !== null
              ? r(String(p.path ?? p.dest ?? p.role ?? "?"), 40)
              : "?",
        _ = (p) => {
          if (Array.isArray(p) && p.length > 0) {
            let C = p.slice(0, 20),
              j = t ? Math.max(0, Math.min(200, Math.floor(1800 / C.length) - 95)) : 0,
              P =
                t &&
                j === 0 &&
                C.some((I) => {
                  if (typeof I !== "object" || I === null) return !1;
                  let z = I;
                  return (typeof z.data === "string" && z.data) || (typeof z.content === "string" && z.content);
                }),
              D = p.slice(20),
              T =
                D.length > 0
                  ? `, +${D.length} more: ${D.slice(0, 10).map(u).join(", ")}${D.length > 10 ? ", \u2026" : ""}`
                  : "";
            return (
              `[${p.length} ${p.length === 1 ? "entry" : "entries"}: ` +
              C.map((I) => o(I, j)).join(", ") +
              T +
              "]" +
              (P ? " (content previews omitted)" : "")
            );
          }
          let v = b(p) ?? String(p);
          return v.length > 80 ? `${r(v)} (${E(N(v))})` : r(v);
        },
        y = Object.entries(c).sort(([, p], [, v]) => Number(Array.isArray(p)) - Number(Array.isArray(v))),
        s = y.filter(([, p]) => Array.isArray(p)),
        d = s.length >= 2 ? ` (${s.map(([p, v]) => `${r(p)}: ${v.length}`).join(", ")})` : "",
        g = y.map(([p, v]) => `${r(p)}: ${_(v)}`).join(", "),
        w = b(c) ?? "",
        k = N(w),
        h = `${k > 256 ? ` (${E(k)} total)` : ""}${d} ${g}`.trimStart(),
        S = t ? 2000 : 1500;
      return h.length > S ? h.slice(0, S - 1).trimEnd() + "\u2026" : h;
    },
  }),
  ze = /^image\/(png|jpeg|gif|webp)$/,
  ye = 130000,
  se = 130000;
function Pe(e) {
  if (e.type === "text" && typeof e.text === "string") return [{ type: "text", text: e.text }];
  if (e.type === "image" && typeof e.data === "string" && typeof e.mimeType === "string" && ze.test(e.mimeType)) {
    if (e.data.length > ye)
      return [
        {
          type: "text",
          text: `[render_preview image omitted \u2014 ${Math.round(e.data.length / 1024)}KB exceeds ${Math.round(ye / 1024)}KB cap]`,
        },
      ];
    return [{ type: "image", source: { type: "base64", media_type: e.mimeType, data: e.data } }];
  }
  return [{ type: "text", text: b(e) ?? "" }];
}
function He(e) {
  return e.type === "image" && e.source.type === "base64"
    ? e.source.data.length
    : e.type === "text"
      ? e.text.length
      : 0;
}
function Fe(e) {
  let t = se,
    n = [],
    r = 0,
    c = 0;
  for (let o of e) {
    let u = He(o);
    if (u <= t) (t -= u), n.push(o);
    else r++, (c += u);
  }
  if (r > 0)
    n.push({
      type: "text",
      text: `[ClaudeDesign result truncated \u2014 ${r} block(s) (${Math.round(c / 1024)}k chars) omitted; aggregate over ${Math.round(se / 1024)}k-char cap]`,
    });
  return n;
}
class X extends Error {
  consent;
  constructor(e) {
    super("Claude Design requires consent");
    this.consent = e;
    this.name = "DesignNeedsConsentError";
  }
}
function xe(e) {
  if (e == null || typeof e !== "object" || e.error !== "needs_consent") return null;
  let t = e.consent;
  return gje(t) ? t : null;
}
class Y extends Error {
  projectId;
  constructor(e) {
    super("Claude Design requires a project write grant");
    this.projectId = e;
    this.name = "DesignNeedsProjectGrantError";
  }
}
function Ne(e) {
  if (e == null || typeof e !== "object" || e.error !== "needs_project_grant") return null;
  let t = e.project_id;
  if (typeof t !== "string" || t.length === 0 || t.length > vWe - 2 || !/^[A-Za-z0-9._-]+$/.test(t)) return null;
  return t;
}
class Z extends R {
  constructor(e) {
    super(
      e?.isNonInteractiveSession === !0
        ? "Claude Design rejected this session's claude.ai credential (HTTP 403): it does not carry Claude Design access, and /design login cannot run in this non-interactive session. Ask the user to run /design login once from an interactive Claude Code session on this machine \u2014 non-interactive runs here then reuse that authorization. (CI runners and hosted sessions have no interactive session; Claude Design is not reachable from those without a stored /design login credential.)"
        : "Claude Design rejected this session's claude.ai credential (HTTP 403): it does not carry Claude Design access. Run /design login to authorize Claude Design, then retry.",
      "design_tool_needs_design_scopes",
    );
    this.name = "DesignNeedsScopesError";
  }
}
var Le = 64;
function we(e, t, n) {
  if ((e.servedCatalogHashes.delete(t), e.servedCatalogHashes.set(t, n), e.servedCatalogHashes.size > Le)) {
    let r = e.servedCatalogHashes.keys().next().value;
    if (r !== void 0) e.servedCatalogHashes.delete(r);
  }
}
function Be(e) {
  let t = e.replace(/\s+/g, " ").trim(),
    n = t.indexOf(". "),
    r = n > 0 ? t.slice(0, n + 1) : t;
  return r.length > 140 ? ce(r, 140).trimEnd() + "\u2026" : r;
}
async function be(e, t, n, r, c, o, u) {
  try {
    let _ = null;
    if (t === o2 || de(t)) _ = await Ue(e, r, c, u);
    if (t === o2) {
      let w = (_ ?? []).map((p) => ({ name: p.name, description: p.description, inputSchema: p.inputSchema })),
        k = b({ tools: w }) ?? "",
        A = Vn(k),
        h = n?.full;
      if (
        !(h !== void 0 && h !== !1 && h !== "false" && h !== 0 && h !== "") &&
        o !== null &&
        e.servedCatalogHashes.get(o) === A
      ) {
        we(e, o, A);
        let p = {
          catalog_unchanged: !0,
          catalog_hash: A,
          note:
            `The operation catalog is unchanged since the earlier "${o2}" result in this conversation (hash ${A}) \u2014 full descriptions and argument schemas are in that result. ` +
            `If it is no longer in context, call ${zG}({operation: "${o2}", arguments: {full: true}}) for the full catalog.`,
          operations: w.map((v) => ({ name: v.name, summary: Be(v.description) })),
        };
        return { operation: t, content: [{ type: "text", text: b(p) }] };
      }
      if (o !== null) we(e, o, A);
      return { operation: t, content: [{ type: "text", text: k }] };
    }
    if (_ && x(t) === void 0)
      return {
        operation: t,
        content: [
          {
            type: "text",
            text: `Unknown Claude Design operation "${t}". Call ${zG}({operation: "${o2}"}) to see the available operations.`,
          },
        ],
        isError: !0,
      };
    let y = (w) => ee({ jsonrpc: "2.0", id: 1, method: "tools/call", params: { name: t, arguments: n } }, r, w, c, u),
      s = await Q(e, r, c, u),
      d = await y(s);
    if (d.status === 404 && s) Se(e), (d = await y(await Q(e, r, c, u)));
    if (d.status === 403) {
      let w = xe(d.data);
      if (w !== null) throw new X(w);
      let k = Ne(d.data);
      if (k !== null) throw new Y(k);
    }
    if (d.status < 200 || d.status >= 300)
      throw new R(`Claude Design ${t} failed: HTTP ${d.status} ${L(b(d.data) ?? "", r)}`, "design_tool_http_error");
    let g = d.data;
    if (g.error)
      return {
        operation: t,
        content: [
          { type: "text", text: L(`${g.error.message}${g.error.data ? ` \u2014 ${b(g.error.data)}` : ""}`, r) },
        ],
        isError: !0,
      };
    return { operation: t, content: g.result?.content ?? [], isError: g.result?.isError };
  } catch (_) {
    if (c.aborted) throw new Ze();
    if (_ instanceof R || _ instanceof X || _ instanceof Y) throw _;
    if (Me(_))
      throw new R(
        `Claude Design ${t} was cancelled because a concurrent Claude Design call was interrupted mid-handshake (the first calls of a session share one initialize/discovery round-trip). Retry the operation.`,
        "design_tool_shared_handshake_cancelled",
      );
    throw new R(L(l(_), r), "design_tool_request_failed");
  }
}
function Me(e) {
  if (typeof e !== "object" || e === null) return !1;
  let t = e;
  return t.name === "CanceledError" || t.name === "AbortError" || t.code === "ERR_CANCELED";
}
function Ue(e, t, n, r) {
  if (e.discoverInFlight) return e.discoverInFlight;
  let c = qe(e, t, n, r);
  return (
    (e.discoverInFlight = c),
    c.finally(() => {
      if (e.discoverInFlight === c) e.discoverInFlight = null;
    })
  );
}
async function qe(e, t, n, r) {
  let c = (_) => ee({ jsonrpc: "2.0", id: 1, method: "tools/list", params: {} }, t, _, n, r),
    o = await Q(e, t, n, r),
    u = await c(o);
  if (u.status === 404 && o) Se(e), (u = await c(await Q(e, t, n, r)));
  if (u.status < 200 || u.status >= 300 || !Array.isArray(u.data.result?.tools))
    throw new R(`Claude Design discovery failed: HTTP ${u.status}`, "design_tool_discovery_failed");
  return le(u.data.result.tools);
}
function Se(e) {
  (e.cachedSessionId = null), (e.initialized = !1), (e.initializeInFlight = null), (e.discoverInFlight = null);
}
async function Q(e, t, n, r) {
  if (e.initialized) return e.cachedSessionId;
  if (e.initializeInFlight) return e.initializeInFlight;
  let c = (async () => {
    let o = await ee(
      {
        jsonrpc: "2.0",
        id: 0,
        method: "initialize",
        params: {
          protocolVersion: "2025-03-26",
          capabilities: {},
          clientInfo: { name: "claude-cli-design-tool", version: "1" },
        },
      },
      t,
      null,
      n,
      r,
      "text",
    );
    if (o.status < 200 || o.status >= 300)
      throw new R(`Claude Design initialize failed: HTTP ${o.status}`, "design_tool_initialize_failed");
    let u = o.response.headers["mcp-session-id"];
    return (e.cachedSessionId = typeof u === "string" && u ? u : null), (e.initialized = !0), e.cachedSessionId;
  })();
  e.initializeInFlight = c;
  try {
    return await c;
  } finally {
    if (e.initializeInFlight === c) e.initializeInFlight = null;
  }
}
class ae extends R {
  wasRetried;
  constructor(e = !1) {
    super(
      "Claude Design authentication failed (HTTP 401). The claude.ai credential is missing or expired \u2014 run /login, or /design login for a separate design credential.",
      "design_tool_auth_401",
    );
    (this.name = "DesignAuth401Error"), (this.wasRetried = e);
  }
}
var ve = 5000;
async function Ge(e, t, n) {
  let r;
  if (O() && n !== void 0) r = await qa(n);
  else r = Yt();
  if (e === r?.accessToken && Boolean(r?.refreshToken)) {
    if ((await Promise.race([ym(e, n).catch(() => !1), ne(ve, t)]), t.aborted)) return null;
  }
  let c = await Promise.race([U0e(n).catch(() => null), ne(ve, t).then(() => null)]);
  return c?.ok === !0 && c.accessToken !== e ? c.accessToken : null;
}
async function We(e, t) {
  let n = t?.isNonInteractiveSession === !0,
    r =
      t?.wasRetried === !0
        ? "Claude Design authentication failed (HTTP 401): a freshly refreshed credential was also rejected \u2014 likely a server-side access problem with this account or credential rather than simple expiry."
        : "Claude Design authentication failed (HTTP 401): the credential was rejected and an automatic refresh did not produce a new one.",
    c;
  if (O() && e !== void 0) c = await qa(e);
  else c = Yt();
  let o = !!c?.accessToken && !!c.refreshToken && c.scopes?.includes("user:design:read") === !0;
  if (!o && (await yUn(e)))
    return `${r} The design credential (from /design login) is expired or revoked${n ? ", and /design login requires an interactive terminal \u2014 re-authenticate outside this session" : " \u2014 run /design login to re-authenticate"}.`;
  if (!o && c?.accessToken && !c.refreshToken) {
    let { source: u } = O() && e !== void 0 ? await T4t(e) : Fl();
    if (a.CLAUDE_CODE_REMOTE_SESSION_ID)
      return `${r} This remote session's credential is injected and rotated by the session host \u2014 it usually self-heals within minutes. Retry shortly; if this persists, the host session needs attention.`;
    if ((u === "CCR_OAUTH_TOKEN_FILE" || u === "CLAUDE_CODE_OAUTH_TOKEN_FILE_DESCRIPTOR") && (!LJ() || Rc()))
      return `${r} This session authenticates with an OAuth token injected by the CCR host, which has no refresh token \u2014 it cannot self-heal and has likely expired or been revoked. The credential comes from the host session; check or restart it there.`;
    if (u === "CLAUDE_CODE_OAUTH_TOKEN" || (u === "ANTHROPIC_AUTH_TOKEN" && Boolean(a.CLAUDE_CODE_OAUTH_TOKEN)))
      return `${r} This session authenticates with the CLAUDE_CODE_OAUTH_TOKEN environment variable, which has no refresh token \u2014 it cannot self-heal and has likely expired or been revoked. Mint a fresh token with \`claude setup-token\` and restart the session with it${n ? "" : ", or unset it and run /login"}.`;
  }
  if (n)
    return `${r} The claude.ai credential is expired or revoked, and /login requires an interactive terminal \u2014 re-authenticate outside this session.`;
  return `${r} Run /login, or /design login for a separate design credential.`;
}
async function ee(e, t, n, r, c, o, u = !1) {
  if (!FT(zt().BASE_API_URL))
    throw Error(
      "Claude Design is only reachable from api.anthropic.com; the current OAuth base URL is not on the first-party allowlist.",
    );
  let _ = await bt.post("/v1/design/mcp", e, {
    auth: "none",
    headers: {
      ...Ae(t),
      Accept: "application/json, text/event-stream",
      "X-Anthropic-Client": "claude-cli-design-tool",
      ...(n && { "Mcp-Session-Id": n }),
    },
    timeout: 60000,
    maxContentLength: 16 * se,
    validateStatus: () => !0,
    signal: r,
    ...(o && { responseType: o }),
  });
  if (!_.ok) throw new R(`Claude Design unavailable: ${_.reason}`, "design_tool_policy_refused");
  if (_.status === 401) {
    if (!u && !r.aborted) {
      let s = await Ge(t, r, c);
      if (s !== null && !r.aborted)
        try {
          let d = await ee(e, s, n, r, c, o, !0);
          if (typeof d.data === "string") {
            if (d.data.includes(s)) d.data = L(d.data, s);
          } else if (d.data !== null && d.data !== void 0) {
            let g = b(d.data);
            if (g !== void 0 && g.includes(s)) d.data = V(L(g, s));
          }
          return d;
        } catch (d) {
          if (d instanceof Error)
            try {
              d.message = L(d.message, s);
            } catch {}
          throw d;
        }
    }
    if (!u && r.aborted) throw new Ze();
    throw new ae(u);
  }
  if (_.status === 403) {
    let s = _.data;
    if (typeof s === "string")
      try {
        s = V(s);
      } catch {
        s = null;
      }
    if (s !== null && typeof s === "object" && s.error === "needs_design_scopes") throw new Z();
  }
  let y = _.response.headers["content-type"];
  if (typeof y === "string" && y.toLowerCase().startsWith("text/event-stream"))
    throw new R(
      "Claude Design returned a text/event-stream response; this client only handles JSON.",
      "design_tool_sse_unsupported",
    );
  return _;
}
function Ve(e, t) {
  let n = e.detail ? ` (${e.detail})` : "";
  switch (e.reason) {
    case "needs_design_login":
      if (t?.isNonInteractiveSession)
        return "Claude Design needs a claude.ai credential, and /design login cannot run in this non-interactive session. Ask the user to run /design login once from an interactive Claude Code session on this machine \u2014 non-interactive runs here then reuse that authorization. (CI runners and hosted sessions have no interactive session; Claude Design is not reachable from those without a stored /design login credential.)";
      return `No design-capable credential${n}. Run /design login to authorize Claude Design.`;
    case "design_refresh_failed":
      return `Design token refresh failed${n}. Retry shortly${t?.isNonInteractiveSession ? "" : ", or run /design login"}.`;
    case "wrong_provider":
      return "Claude Design is only available with claude.ai authentication. It is not supported through Bedrock, Vertex, or other third-party providers.";
    case "essential_traffic_only":
      return "Claude Design is disabled because nonessential network traffic is restricted.";
  }
}
JUn({
  isEnabled: hUn,
  createObserver: (e, t, n) => (e.kind === "design_project_grant" ? wUn(t.get(HA), e.projectId, n) : null),
});
export { $t as DesignTool, $e as operationLabel };
